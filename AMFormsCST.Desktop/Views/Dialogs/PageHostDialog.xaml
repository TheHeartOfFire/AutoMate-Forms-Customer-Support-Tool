<ui:FluentWindow x:Class="AMFormsCST.Desktop.Views.Dialogs.PageHostDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:AMFormsCST.Desktop.ViewModels.Dialogs"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="{Binding DialogTitle}"  Height="Auto" Width="Auto" MinHeight="500" MinWidth="400"
        SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen"
        WindowStyle="ToolWindow" Topmost="True" WindowBackdropType="Mica"
        d:DataContext="{d:DesignInstance viewModels:PageHostDialogViewModel}"
        ExtendsContentIntoTitleBar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <Grid Name="CustomTitleBarArea" Height="32" VerticalAlignment="Top" Background="Transparent" MouseLeftButtonDown="CustomTitleBarArea_MouseLeftButtonDown" />

        <Frame x:Name="DialogPageFrame" Grid.Row="1" NavigationUIVisibility="Hidden" Margin="5"/>

        <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Right" Orientation="Horizontal" Margin="15" Grid.Row="2">
            <ui:Button Command="{Binding ConfirmCommand}" Margin="0,0,10,0" IsCancel="True" Padding="0"
                       CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ui:FluentWindow}}}">
                <ui:SymbolIcon Symbol="CheckboxChecked24" FontSize="32"/>
            </ui:Button>
            <ui:Button Command="{Binding CloseCommand}" Margin="0,0,10,0" IsCancel="True" Padding="0"
                       CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ui:FluentWindow}}}">
                <ui:SymbolIcon Symbol="DismissSquare24" FontSize="32"/>
            </ui:Button>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
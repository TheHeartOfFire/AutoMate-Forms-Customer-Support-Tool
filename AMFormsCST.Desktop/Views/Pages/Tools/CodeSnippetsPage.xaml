<Page
    x:Class="AMFormsCST.Desktop.Views.Pages.Tools.CodeSnippetsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:AMFormsCST.Desktop.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:AMFormsCST.Desktop.Views.Pages.Tools"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:AMFormsCST.Desktop.ViewModels.Pages.Tools"
    Title="CodeSnippetsPage"
    d:DataContext="{d:DesignInstance Type=viewmodels:CodeSnippetsViewModel,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Code Snippet List  -->
        <ListBox
            Grid.Column="0"
            ItemsSource="{Binding CodeSnippets}"
            SelectedItem="{Binding SelectedCodeSnippet, Mode=TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Label Content="{Binding Name, Mode=OneWay}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!--  Selected Snippet Details  -->
        <Grid Grid.Column="1" Margin="10,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                FontSize="16"
                FontWeight="Bold"
                Text="{Binding SelectedCodeSnippet.Name}"  Foreground="White"/>
            <TextBlock
                Grid.Row="1"
                Margin="0,5,0,10"
                Text="{Binding SelectedCodeSnippet.Description}"
                TextWrapping="Wrap"  Foreground="White"/>

            <!--  Inputs Section with constrained height -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding SelectedCodeSnippet.Inputs}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <ui:TextBox HorizontalAlignment="Stretch" PlaceholderText="{Binding Description}" Margin="0,0,10,0" Text="{Binding BindableValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>

            <StackPanel
                Grid.Row="3"
                Margin="0,10,0,5"
                Orientation="Horizontal">
                <ui:TextBlock
                    VerticalAlignment="Center"
                    FontWeight="Bold"
                    Text="Output" Foreground="White"/>
                <Button
                    Margin="10,0,0,0"
                    Command="{Binding CopyOutputCommand}"
                    Content="Copy" />
            </StackPanel>

            <ui:TextBox
                Grid.Row="4"
                AcceptsReturn="True"
                IsReadOnly="True"
                Text="{Binding SelectedCodeSnippet.Output}"
                TextWrapping="Wrap"
                VerticalScrollBarVisibility="Auto" 
                IsReadOnlyCaretVisible="True"/>
        </Grid>
    </Grid>
</Page>

<Page x:Class="AMFormsCST.Desktop.Views.Pages.Tools.CodeSnippetsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" 
      xmlns:viewModels="clr-namespace:AMFormsCST.Desktop.ViewModels.Pages.Tools"
      xmlns:snippetViewModels="clr-namespace:AMFormsCST.Desktop.ViewModels.Pages.Tools.CodeSnippets"
      xmlns:codeBlocks="clr-namespace:AMFormsCST.Core.Interfaces.CodeBlocks;assembly=AMFormsCST.Core"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CodeSnippetsPage" 
      d:DataContext="{d:DesignInstance viewModels:CodeSnippetsViewModel, 
                                 IsDesignTimeCreatable=False}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Column="0" Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalAlignment="Stretch">
            <ItemsControl ItemsSource="{Binding CodeSnippets}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="vertical" IsItemsHost="True" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type snippetViewModels:CodeSnippetItemViewModel}">
                        <ToggleButton
                            IsChecked="{Binding IsSelected}"
                            Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type Page}}, Path=DataContext.SelectCodeSnippetCommand}"
                            CommandParameter="{Binding}"
                            Margin="5"
                            Padding="0"
                            Width="360">
                            <ToggleButton.Content>
                                <Border BorderBrush="DarkGray" BorderThickness="2" CornerRadius="5" Background="#25000000">
                                    <WrapPanel Orientation="Vertical" MaxWidth="350" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="5" HorizontalAlignment="Center" FontSize="18"/>
                                        <Separator Margin="5,0,5,0"/>
                                        <TextBlock Text="{Binding Description}" Margin="5" TextWrapping="Wrap" HorizontalAlignment="Center"/>
                                    </WrapPanel>
                                </Border>
                            </ToggleButton.Content>
                        </ToggleButton>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.Resources>
                    <Style TargetType="RadioButton" BasedOn="{StaticResource {x:Type ToggleButton}}"/>
                </ItemsControl.Resources>
            </ItemsControl>
        </ScrollViewer>
        <WrapPanel Grid.Column="1" Grid.Row="0" Orientation="Vertical">
        <Border Background="#1AFFFFFF" BorderBrush="LightGray" BorderThickness="1" Margin="5,5,0,5" CornerRadius="5" VerticalAlignment="Top">
            <ItemsControl ItemsSource="{Binding SelectedCodeSnippet.Inputs}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" IsItemsHost="True" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type snippetViewModels:CodeInputViewModel}">
                            <WrapPanel Margin=" 10,0,10,0">
                                <ui:TextBox PlaceholderText="{Binding Description}" Text="{Binding BindableValue,UpdateSourceTrigger=PropertyChanged}"/>
                            </WrapPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            
        </Border>
            <WrapPanel Orientation="Horizontal">
            <ui:TextBox Text="{Binding SelectedCodeSnippet.Output, Mode=OneWay}" IsReadOnly="True" IsReadOnlyCaretVisible="True" Margin="5,5,0,0"/>
                <ui:Button Margin="5,5,0,0" Command="{Binding CopyOutputCommand}">
                    <!--To open Code Snippets-->
                    <ui:SymbolIcon Symbol="Copy20" VerticalAlignment="Top" HorizontalAlignment="Center" FontSize="24"/>
                </ui:Button>
            </WrapPanel>
        </WrapPanel>
    </Grid>
</Page>

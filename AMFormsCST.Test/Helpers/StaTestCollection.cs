using Xunit;

[CollectionDefinition("STA Tests", DisableParallelization = true)]
public class StaTestCollection
{
    // This class has no code, and is never created. Its purpose is just for xUnit's collection definition.
}